apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task01-pv-claim
  namespace: dev
  # annotations:
  #   volume.beta.kubernetes.io/storage-class: ""
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi